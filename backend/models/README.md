# Place your trained YOLOv8 model weights file here.
#
# Filename: best.pt
#
# To train a custom model:
#   cd ml/
#   python train.py
#
# The model must have classes that include "bottle" and "can" (case-insensitive).
# If this file is absent, the backend falls back to the COCO pretrained yolov8n.pt,
# which can detect bottles but uses an aspect-ratio heuristic for cans.
